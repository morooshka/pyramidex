# Pyramidex - Project Conventions

## File naming

All `.md` and `.yaml` files use kebab-case (e.g. `migrate-export.md`, `dump-schema.yaml`).
Exception: well-known uppercase root files (`README.md`, `CLAUDE.md`, `CHANGELOG.md`).
